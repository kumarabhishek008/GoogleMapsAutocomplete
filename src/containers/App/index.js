import React from 'react'
import SearchLocation from '../Search/index.js'

const AppContainer = (props) => {
  return (
    <div>
        <SearchLocation {...props}/>
    </div>
  )
}

export default AppContainer